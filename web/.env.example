# Site
NEXT_PUBLIC_SITE_URL=https://eddie.kartersanamo.com

# Database (MySQL on the same server as Docker)
# Use 127.0.0.1 — deploy.sh runs the container with host networking so localhost works.
DATABASE_URL="mysql://user:password@127.0.0.1:3306/eddie_youman"

# File uploads (job photos) — not stored in the database
# Local: ./data   Docker: /app/data (mounted volume in deploy.sh)
DATA_DIR=./data

# Optional: override host path for upload volume (deploy.sh only)
# DATA_DIR_HOST=./data

# Auth (generate with: openssl rand -base64 32)
AUTH_SECRET=
AUTH_URL=https://eddie.kartersanamo.com

# Admin seed (used by npm run db:seed on first deploy only)
# Comma-separated emails; ADMIN_PASSWORD is a one-time temporary password per account
# (must be changed on first login). Re-running seed does not reset existing passwords.
ADMIN_EMAILS=eddie@eddieyouman.com
ADMIN_PASSWORD=ChangeMeNow!

# Mailgun (required for contact form and quote requests)
MAILGUN_API_KEY=
MAILGUN_DOMAIN=
MAILGUN_FROM="Eddie Youman Services <noreply@your-mailgun-domain>"
CONTACT_TO_EMAIL=eddie@eddieyouman.com

# Stripe (payments on completed jobs — Checkout redirect; publishable key unused)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Rate limits
CONTACT_RATE_LIMIT_PER_HOUR=5

# Cron (required in production — Bearer token for /api/cron/* routes)
CRON_SECRET=
